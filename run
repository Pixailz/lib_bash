#!/bin/bash

## ⚠️ TEST ZONE ⚠️

function	test::entry()
{
	if [ ${#} -eq 0 ]; then
		ansi::load "test loading" "6"
	else
		ansi::down "test downloading" "0" "6"
	fi
}

## ⚠️ END TEST ZONE ⚠️

source ./.config

parse::entry ${@}

test::entry ${@}
